export enum PaymentTypeEnum {
    cash = "cash",
    card = "card"
}


export enum OrderStatusEnum {
    pending = "pending",
    placed = "placed",
    onWay = "on-way",
    canceled = "canceled",
    delivered = "delivered",
}