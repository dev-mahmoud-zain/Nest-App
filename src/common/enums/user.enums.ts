export enum GenderEnum {
    male = "male",
    female = "female"
}

export enum ProviderEnum {
    system = "system",
    google = "google"
}

export enum RoleEnum {
    user = "user",
    admin = "admin",
    super_admin = "super_admin",
}